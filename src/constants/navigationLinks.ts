const marketplace = {
  label: 'Marketplace',
  url: '/marketplace',
};

const mint = {
  label: 'Mint',
  url: '/mint',
};

const profile = {
  label: 'Profile',
  url: '/profile',
};

export const unauthorizedLinks = [marketplace, mint];
export const authorizedLinks = [marketplace, mint, profile];
